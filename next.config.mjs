/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "drive.google.com",
      "github.githubassets.com",
      "upload.wikimedia.org",
      "1000logos.net",
    ],
  },
};

export default nextConfig;
